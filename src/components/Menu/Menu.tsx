const Menu = () : JSX.Element => {
    return (
        <ul className="navPages">
            <li>Find a transaction</li>
            <li>New transaction</li>
        </ul>
    );
}

export default Menu;